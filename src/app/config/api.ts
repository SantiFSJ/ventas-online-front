import { environment } from '../../environments/environment';

export const ventaUrl = environment.baseUrl + 'ventas';

export const clienteUrl = environment.baseUrl + 'clientes';

export const descuentoUrl = environment.baseUrl + 'descuentos';

export const productoUrl = environment.baseUrl + 'productos';

export const categoriaUrl = environment.baseUrl + 'categorias';